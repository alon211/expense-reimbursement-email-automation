# 核心依赖（必须安装）
pytz>=2024.1          # 时区处理（解决定时任务时区不一致问题）
requests>=2.31.0      # HTTP请求（钉钉/企业微信/飞书推送必备）

# 可选依赖（根据需求选择）
imaplib2>=3.6         # 增强IMAP兼容性（解决部分邮箱连接不稳定问题）
python-dotenv>=1.0.0  # 环境变量管理（可把敏感配置放.env文件，替代config.py）
chardet>=5.2.0        # 邮件编码自动检测（解决中文邮件乱码）
pycryptodome>=3.20.0  # 加密相关（部分企业邮箱IMAP需要）
py7zr>=0.20.0         # 7z压缩文件解压（压缩文件自动解压功能）
rarfile>=4.0          # rar压缩文件解压（压缩文件自动解压功能）